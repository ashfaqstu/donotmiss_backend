# Database Configuration
# For local development, use SQLite (default)
# For production on Render, this is automatically set from the PostgreSQL addon
DATABASE_URL=sqlite:///donotmiss.db
# Example PostgreSQL URL: postgresql://user:password@host:port/database

# Jira API Configuration (optional - only needed if backend creates Jira issues directly)
# Get your API token from: https://id.atlassian.com/manage-profile/security/api-tokens

JIRA_SITE=ahammadshawki8.atlassian.net
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-api-token-here
JIRA_PROJECT_KEY=DNM
